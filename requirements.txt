# Mosaic / Tessera — direct dependencies.
# Python 3.12 recommended (self-hosted local stack); developed/verified on 3.14.3.
# Recreate the env with:  python -m venv .venv && .venv/bin/pip install -r requirements.txt
cognee==1.2.2
python-dotenv==1.0.1
fastembed==0.8.0    # local embeddings (no API/quota) — the "re-embeds locally" path
mcp==1.28.1         # MCP server SDK — the consult_pack endpoint (primary access path)
